# Falcon screen on INVITE. Load this after http_client and json.
# The script posts selected headers and applies X-Falcon-* plus a 603 on reject.

loadmodule "http_client.so"
loadmodule "jansson.so"

modparam("http_client", "connection", "falcon=>http://127.0.0.1:8090;")
# Set http_client timeout low. Fail-open is the Falcon default.

route[FALCON_SCREEN] {
    if (!is_method("INVITE")) {
        return;
    }

    $var(body) = "{";
    jansson_set("string", "switch", "kamailio", "$var(body)");
    jansson_set("string", "method", "INVITE", "$var(body)");
    jansson_set("string", "request_uri", "$ru", "$var(body)");
    jansson_set("string", "source_ip", "$si", "$var(body)");
    jansson_set("string", "headers.From", "$hdr(From)", "$var(body)");
    jansson_set("string", "headers.To", "$hdr(To)", "$var(body)");
    jansson_set("string", "headers.Call-ID", "$ci", "$var(body)");
    jansson_set("string", "headers.User-Agent", "$hdr(User-Agent)", "$var(body)");
    jansson_set("string", "headers.P-Asserted-Identity", "$hdr(P-Asserted-Identity)", "$var(body)");
    jansson_set("string", "headers.Identity", "$hdr(Identity)", "$var(body)");
    jansson_set("string", "headers.Contact", "$ct", "$var(body)");
    jansson_set("string", "headers.Max-Forwards", "$hdr(Max-Forwards)", "$var(body)");

    if (http_connect("falcon", "/v1/screen", "application/json", "$var(body)", "$var(falcon)") < 0) {
        xlog("L_WARN", "Falcon unreachable, fail-open for $ci\n");
        return;
    }

    if (!jansson_get("action", "$var(falcon)", "$var(action)")) {
        return;
    }

    jansson_get("risk_score", "$var(falcon)", "$var(score)");
    append_hf("X-Falcon-Action: $var(action)\r\n");
    append_hf("X-Falcon-Score: $var(score)\r\n");

    if ($var(action) == "reject" || $var(action) == "challenge") {
        jansson_get("sip_status", "$var(falcon)", "$var(code)");
        if ($var(code) == 0 || $var(code) == "") {
            $var(code) = 603;
        }
        send_reply("$var(code)", "Falcon");
        exit;
    }
}

# Optional: report how the call ended, so Falcon can score answer rate and
# talk time per caller and per customer. Needs the dialog module.
#
#   loadmodule "dialog.so"
#   modparam("dialog", "dlg_flag", 4)
#
# In request_route, for INVITEs that passed FALCON_SCREEN:
#   setflag(4); dlg_manage();
#
# Outbound calls from your own customers: set the direction and account
# before route(FALCON_SCREEN) so Falcon checks the caller id against the
# numbers registered for that account:
#   $var(direction) = "outbound"; $var(customer) = $au;
# and add to the JSON body in FALCON_SCREEN:
#   jansson_set("string", "direction", "$var(direction)", "$var(body)");
#   jansson_set("string", "customer", "$var(customer)", "$var(body)");
#
# event_route[dialog:end] {
#     $var(dur) = $TS - $dlg(start_ts);
#     $var(answered) = "false";
#     if ($dlg(state) == 4) { $var(answered) = "true"; }
#     $var(out) = "{\"call_id\":\"" + $ci + "\",\"answered\":" + $var(answered) + ",\"duration_s\":" + $var(dur) + "}";
#     http_connect("falcon", "/v1/outcome", "application/json", "$var(out)", "$var(res)");
# }
#
# Kamailio has no media, so audio clips come from the media server behind
# it (rtpengine recording or the B2BUA). Post them to /v1/audio?call_id=.
