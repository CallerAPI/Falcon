# How long Prometheus keeps scraped samples. Examples: 15d, 30d, 90d.
PROMETHEUS_RETENTION=30d

# Required. Grafana stores this on first start. A later change does not
# replace the password already in the volume.
GRAFANA_ADMIN_PASSWORD=change-me

GRAFANA_PORT=3000
